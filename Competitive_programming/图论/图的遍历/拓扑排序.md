有向图才有拓扑序列

有环的话就没有拓扑序了

可以证明，一个有向无环图，一定存在拓扑序列。因此，有向无环图也被称为拓扑图

这里还有一个概念：度数，图里每一个点都有两个度，入度（有多少条边指向自己）和出度（有多少条边指向别的点）

性质：一个有向无环图，至少存在一个入度为0的点（证明：反证法）

求拓扑序的算法：

```cpp
queue<--所有入度为0的点
while(!queue.empty())
{
	t<-对头
	枚举t的所有出边
		删掉t->j, j的入度-1;
		if(j的入度 == 0)
			queue<-j
}
	
```
